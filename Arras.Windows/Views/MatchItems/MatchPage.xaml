<Page
    x:Class="Arras.Windows.Views.MatchItems.MatchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Arras.Windows.Views.MatchItems"
    xmlns:reuse="using:Arras.Windows.Views.ReuseableComponents"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource backgroundColor}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".8*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TOP ROW - SCORE ITEMS-->
        <Grid Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width=".5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <reuse:MatchScoreItem Margin="20"/>
            <reuse:MatchScoreItem Margin="20" Grid.Column="2"/>
        </Grid>

        <!--BOTTOM ROW - STATS AND KEYBOARD -->
        <Grid Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width=".9*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid>
                
            </Grid>
            <reuse:Keyboard Grid.Column="1" Margin="20"/>
            <Grid>
                
            </Grid>
        </Grid>
    </Grid>

</Page>
