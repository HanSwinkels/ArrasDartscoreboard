<UserControl
    x:Class="Arras.Windows.Views.ReuseableComponents.MatchScoreItemInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Arras.Windows.Views.ReuseableComponents"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid MaxHeight="100" MaxWidth="100"  Background="{ThemeResource whiteColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <Border CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource primaryColor}">
            <TextBlock Text="{x:Bind Info}" FontWeight="Bold" FontSize="18" FontFamily="Segoe UI" Margin="8,4" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{ThemeResource whiteColor}"/>
        </Border>
        <TextBlock Margin="8,0,0,0" FontFamily="Segoe UI" VerticalAlignment="Center" Text="17.65" Grid.Row="1" Foreground="{ThemeResource textColor}" FontSize="18"/>
    </Grid>
</UserControl>
